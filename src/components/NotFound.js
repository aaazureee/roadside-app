import React from 'react'
import MainLanding from './MainLanding'
const titleText = `Well, that didn't go as planned...`
const bodyText = `Sorry, we can't find the page you're looking for.`

const NotFound = () => <MainLanding titleText={titleText} bodyText={bodyText} />

export default NotFound
